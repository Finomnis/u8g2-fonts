- Render from format_args!()
- Tests for render_args
- Tests for render_args_aligned

- Implement get_args_dimensions
- Implement get_aligned_args_dimensions
- Tests for get_args_dimensions
- Tests for get_aligned_args_dimensions

- Rewrite renderer, use a prettier structure

    - Problem: how to structure?

        current:
            - render_glyph
            - render_text
            - render_text_aligned
            - get_glyph_dimensions
            - get_text_dimensions
            - get_aligned_text_dimensions

        functionality:
            - glyph/text/args   - no influence on rest, should be first
            - alignment vert / vert+hor   - no influence on outcome, but on algorithm
            - color/background/only_dimensions (e.g. color/background optional; only-dimensions does not require display)
                    - influence on result type, should cause a generic to change
            - finished with .draw(display)/.compute();

        maybe:
            .with_content(glyph/text/args)
            .

        or:
            .render_glyph()/.render_text()/.render_args()
            Returns drawbuilder with generic holding the data


- Docs front page
- Docs examples?


Maybe later, if requested:
- Rotation (like in u8g2)
